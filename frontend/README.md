Ghid de pornire proiect:

- env example backend pentru coneexiune la baza de date mongo
- node env sa fie development
- credentiale google din consola google cu 0auth
- directions key se obtine din openrouteservice

- cd ./frontend, npm i, npm run dev
- cd ./backend, npm i, npm run dev
